{
  "name": "ts-sandbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "tsc --noEmit && tsx",
    "watch": "tsx watch",
    "build": "tsc",
  "serve": "node dist/index.js",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
